import { Routes } from '@angular/router';
import { RegisterComponent } from 'app/register/views/register/register.component';

export const ROUTES : Routes = [
  {
    path: 'register',
    component: RegisterComponent
  }
];